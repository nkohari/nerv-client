import { default as coinSymbol } from './coinSymbol';
import { default as decimal } from './decimal';
import { default as hashrate } from './hashrate';
import { default as integer } from './integer';
import { default as percentage } from './percentage';

export const cells = {
  coinSymbol,
  decimal,
  hashrate,
  integer,
  percentage
};
